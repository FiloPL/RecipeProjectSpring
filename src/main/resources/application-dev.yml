spring:
  datasource:
    url: jdbc:mysql://localhost:3306/filo_prod
    username: filo_prod_user
    password: filoprod
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
