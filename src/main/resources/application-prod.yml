spring:
  datasource:
    url: jdbc:mysql://localhost:3306/filo_dev
    username: filo_dev_user
    password: filodev
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true